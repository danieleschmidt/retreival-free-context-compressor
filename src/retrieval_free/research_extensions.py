Resolved the conflict by combining both versions intelligently, using the modern Python 3.10+ type hints (`list`, `dict`, `tuple` instead of `List`, `Dict`, `Tuple`) and better formatting from the PR branch.
